name: ui_animated_size_and_opacity_flutter
description: "Animates size and opacity of a widget in Flutter."
version: 0.0.1
home_page: http://www.icodeforyou.com
publish_to: none # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=3.2.5 <4.0.0" # % flutter --version => values of "Dart"
  flutter: ">=3.16.8"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.4
  ui_extensions_flutter:
    git: https://github.com/GitHubStuff/ui_extensions_flutter.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
